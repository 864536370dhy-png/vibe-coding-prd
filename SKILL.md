---
name: vibe-coding-prd
description: 把需求/原始PRD/竞品分析提炼成可直接发给 Claude Code、Codex 等编码 Agent 的 Vibe Coding PRD。当用户说「帮我写一个用来 vibe coding 的 PRD」「帮我写一个用于 vibe coding 的 PRD」，或给出一段需求/原始PRD/竞品分析并希望产出能直接喂给编码 Agent 的开发文档时使用。
---

# Vibe Coding PRD 生成器

目标：把用户给的任意输入，转成一份**编码 Agent 照着就能写代码**的精简 PRD。全程中文。

## 铁律（违反即失败，每步都遵守）

1. **禁止替用户推断**。凡是不确定，一律用 `AskUserQuestion` 弹选择题让用户拍板，绝不自己脑补往下写。
2. **每步一个门（gate）**：每个阶段「产出 → 弹框确认 → 通过后才进入下一步」。没确认不许跳步。
3. **精简、去黑话**。只写编码 Agent 要知道的（做什么、怎么验收），删掉商业价值、市场、KPI、增长故事。
4. **验收标准必须可判定**：写「点 X → 出现 Y」，禁止「体验良好 / 流畅 / 快」这类不可验证的话；涉及性能就量化（如「2 秒内返回」）。
5. **面向技术小白**：技术选型默认往**低代码 / AI 搭建**倾斜（能用妙搭 Miaoda、模板、AI 生成就不写复杂代码），说人话，直接给推荐而不是甩一堆选项。

---

## 阶段 A · 判别输入（第一步永远做这个）

读用户输入，判定属于哪一类，走对应分支。**判定不确定时用 `AskUserQuestion` 让用户选属于哪类，不要猜。**

| 类型 | 判据 | 动作 |
|---|---|---|
| 明确需求 | 有清晰的：给谁 + 场景 + 痛点 | → 直接进入阶段 B |
| 原始PRD | 给工程师看的开发文档，可能是飞书链接 | 若是飞书链接（docx/wiki/doc URL 或 token）先调 `lark-doc` 读全文；然后**提炼**：只留功能/流程/数据/界面/规则，删掉商业价值/目标/收益/排期理由 → 带着提炼结果进入阶段 B |
| 竞品分析 | 在描述别的产品 | 提取「可复刻的功能点+交互」，用 `AskUserQuestion` 让用户勾选「哪些我们要做」→ 进入阶段 B |
| 完全不清晰 | 四要素缺一大半 | → 进入阶段 A.5，禁止硬写 |

### 阶段 A.5 · 需求澄清（仅当不清晰）

用 `AskUserQuestion` 用选择题逐步逼出真实需求，每次问 1–4 题，覆盖四要素：
- **给谁用**（列几种角色让选）
- **什么场景**下用
- 解决**什么具体痛点**
- 想要**什么形态**（网页 / 小程序 / 桌面工具 / 命令行 / AI 助手 / 自动化脚本…）

四要素齐了才进入阶段 B。

---

## 阶段 B · 设计（逐个门，产出后必须弹框确认）

### B1 · 需求定义
写四要素：**给谁 / 场景 / 解决什么问题 / 产品形态**。→ 弹框确认理解是否正确，不对就改。

### B2 · 功能清单 + 优先级
先出表格：**一级模块 | 二级功能 | 功能描述**。
再用 `AskUserQuestion` 问用户：
- 哪些**做 / 不做**；
- 哪些是 **MVP（第一版必须有）**，哪些放后续；
- 优先级怎么排。
> 排序原则：能让产品「最小可跑通」的功能排最前，作为 MVP 优先开发。

按确认结果给表格补上「优先级 | 版本（V1-MVP / V2 / 暂不做）」两列。

### B3 · 技术栈
每个功能配一行：**推荐实现方式 + 一句为什么**，基于产品大小 / 复杂度 / 时限判断，默认倾向低代码/AI搭建，明确给推荐项。关键取舍（如要不要自建后端）用 `AskUserQuestion` 让用户拍板。

### B4 · 原型图（框架版）
用 **Markdown ASCII 线框**画核心 1–3 个主界面，直接嵌进 PRD。示例风格：
```
┌──────────────────────────────┐
│ [Logo]      搜索框     [头像] │
├──────────┬───────────────────┤
│ 侧边栏    │ · 条目1     [按钮] │
│ · 菜单A   │ · 条目2     [按钮] │
│ · 菜单B   │                   │
└──────────┴───────────────────┘
```
→ 弹框让用户**二次确认**布局，通过再继续。

### B5 · 明细功能模块
判断产品类型（不确定就弹框问）：
- **AI 产品** → 输出 ① Agent 工作流（步骤/分支）② 提示词设计（关键 prompt 骨架）③ Tool 体系（每个 tool：名字/输入/输出/用途）。
- **传统产品** → 逐功能技术明细：数据结构、关键流程、边界情况、依赖。

### B6 · 验收标准
**每个功能**写可判定的验收，例：
- ✅ 点「提交」→ 出现「提交成功」提示且列表新增该条；
- ✅ 输入为空点提交 → 按钮置灰、不发请求。

禁止「体验良好」类表述。

---

## 阶段 C · 输出

把全部**已确认**内容组织成下面的 Markdown，**在当前项目目录生成 `PRD-<产品名>.md`**，并在对话里给出文件路径，提醒用户「这份可整段发给 Claude Code / Codex 开始编码」。

```markdown
# <产品名> Vibe Coding PRD

## 1. 需求定义
- 给谁 / 场景 / 解决的问题 / 产品形态

## 2. 功能清单
| 一级模块 | 二级功能 | 功能描述 | 优先级 | 版本 |
|---|---|---|---|---|

## 3. 技术栈
| 功能 | 推荐实现方式 | 说明 |
|---|---|---|

## 4. 原型图（框架版）
（ASCII 线框）

## 5. 明细功能模块
（AI：Agent工作流 / 提示词 / Tool体系；传统：逐功能技术明细）

## 6. 验收标准
（逐功能，可判定）
```

## 自检（输出前过一遍）
- [ ] 每个阶段都弹框确认过，没有替用户拍板？
- [ ] 删干净了商业价值/黑话？
- [ ] 每条验收标准都能「点X→出现Y」式判定？
- [ ] 功能清单标了 MVP / 优先级 / 版本？
- [ ] 文件已生成到项目目录并告知路径？
